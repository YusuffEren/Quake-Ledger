__pycache__/
*.pyc
.env
*.tfstate
*.tfstate.backup
.terraform/
*.egg-info/
dist/
build/
# GCP credentials — asla commit edilmemeli
gcp-key.json
.gcloud/

# dbt build artifacts
dbt/project/target/
dbt/project/logs/
dbt/project/.user.yml

# Python test cache — pytest calistirma artifact'i, commit edilmemeli
.pytest_cache/

# Coverage artifact'lari — `make coverage` / `make test` yan urunu
.coverage
htmlcov/
.coverage.*
